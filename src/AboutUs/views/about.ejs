<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= title %> — IT Solutions
  </title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Verdana, sans-serif;
      line-height: 1.6;
      color: #333;
      background: #fff;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    ul {
      list-style: none;
    }

    .container {
      width: 90%;
      max-width: 1000px;
      margin: 0 auto;
      padding: 2rem 0;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 0;
    }

    header nav a {
      margin-left: 1.5rem;
      font-weight: 500;
    }

    .btn {
      display: inline-block;
      background: #000;
      color: #fff;
      padding: .75rem 1.5rem;
      border-radius: 25px;
      font-weight: 500;
      transition: background .3s;
    }

    .btn:hover {
      background: #333;
    }

    .hero {
      text-align: center;
      padding: 4rem 0;
      position: relative;
    }

    .hero h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .hero p {
      font-size: 1.125rem;
      max-width: 700px;
      margin: 0 auto 2rem;
      color: #555;
    }

    .hero::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 50%;
      width: 300px;
      height: 300px;
      transform: translate(-50%, -50%);
      z-index: -1;
      background: radial-gradient(circle at center, #ffea80 0%, transparent 70%);
    }

    .cards {
      display: grid;
      grid-template-columns: 1fr;
      gap: 2rem;
      margin: 3rem 0;
    }

    @media(min-width:700px) {
      .cards {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media(min-width:1000px) {
      .cards {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    .card {
      background: #f8f8f8;
      padding: 2rem;
      border-radius: 8px;
    }

    .card h3 {
      margin-bottom: .75rem;
      font-size: 1.25rem;
    }

    .card p {
      font-size: .95rem;
      color: #555;
    }

    .map-section {
      margin: 3rem 0;
      text-align: center;
    }

    #map {
      width: 100%;
      height: 400px;
      border-radius: 15px;
    }

    .cta {
      text-align: center;
      padding: 4rem 0;
      position: relative;
      margin-top: 4rem;
    }

    .cta h2 {
      font-size: 2rem;
      margin-bottom: 1.5rem;
    }

    .cta::before,
    .cta::after {
      content: "";
      position: absolute;
      border-radius: 50%;
      opacity: .4;
    }

    .cta::before {
      width: 200px;
      height: 200px;
      top: -50px;
      left: 10%;
      background: radial-gradient(circle at center, #ff7eb3, transparent 70%);
    }

    .cta::after {
      width: 180px;
      height: 180px;
      bottom: -40px;
      right: 10%;
      background: radial-gradient(circle at center, #7afcff, transparent 70%);
    }
  </style>
</head>

<body>
  <div class="container">
    <header>
      <div class="logo"><strong>IT Solutions</strong></div>
      <nav>
        <a href="/services">Services</a>
        <a href="/about">About Us</a>
        <a href="/contact" class="btn">Let's Talk</a>
      </nav>
    </header>

    <section class="hero" id="about">
      <h1>About Us</h1>
      <p>At IT Solutions, we’re dedicated to delivering innovative, reliable technology services that drive business
        growth and security.</p>
      <a href="#contact" class="btn">Get in Touch</a>
    </section>

    <section class="cards">
      <div class="card">
        <h3>Our Vision</h3>
        <p>To lead the industry by providing top-tier IT solutions that empower companies with secure and scalable
          infrastructure.</p>
      </div>
      <div class="card">
        <h3>Our Mission</h3>
        <p>To tailor technology services that meet the unique needs of each client, fostering innovation and continuous
          support.</p>
      </div>
      <div class="card">
        <h3>Our Values</h3>
        <p>Integrity, innovation, transparency, and customer focus guide everything we do.</p>
      </div>
    </section>

    <section class="map-section">
      <h2>Our Location</h2>
      <div id="map"></div>
    </section>

    <section class="cta" id="contact">
      <h2>Ready to elevate your IT infrastructure?</h2>
      <a href="/contact" class="btn">Contact Us Now</a>
    </section>
  </div>

  <script>
    function initMap() {
      var location = { lat: 21.422960, lng: 39.828855 };
      var map = new google.maps.Map(document.getElementById('map'), {
        center: location,
        zoom: 17,
        mapTypeId: 'satellite' 
      });
      var marker = new google.maps.Marker({ position: location, map: map });
    }
  </script>
  <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDsaPkISPfrYeVc_1T1B_zih_PSuXZfW4k&callback=initMap"></script>
</body>

</html>